---
date: 2026-02-19
tags:
  - 개발/DevLog
projects:
  - vizend-qra-backend
---

# 2026-02-19 DevLog

## vizend-qra-backend

### 장애 감지 및 알림 시스템 (#51, #52)

- Sliding-window incident threshold rule engine 구현 (#51) — 연속 장애 판정 로직
- Notification service adapter with retry 구현 (#52) — 알림 발송 + 재시도
- 알림 executor 분리 및 NotReady duration 판정 정합화
- Threshold coverage 완성 및 notification mapping 연동

### 배포 성공 판정 강화

- 배포 성공을 consecutive K8s readiness 기반으로 gate — 일시적 ready 상태에 속지 않도록 개선
- 초기 배포 모니터링 SSE snapshot 전송 안정화

### K8s Manifest 검증 체계 구축 (#71)

- K8s manifest schema validation before push 추가
- 템플릿 placeholder allowlist + sample render 검증
- manifest 템플릿 및 K8s schema validation 강화

### SSE 개선

- 신규 배포 생성 시 snapshot broadcast 기능 추가
- 배포 실패 reason을 SSE로 전파
- SSE periodic broadcast 실행을 전용 executor로 라우팅

### 설정 및 인프라

- `qra.*` 설정을 `vizend.qra.*` 네임스페이스로 마이그레이션
- Spring configuration metadata 생성 활성화
- application configuration 파일 정리
- observation properties 하드닝 및 async 테스트 정합

### Notification 리팩토링

- dovi Email 구성 로직을 `NotificationProxy` 내부로 캡슐화

### 문서화

- SSE/K8s 모니터링 문서를 현재 구현에 맞게 정렬
- GitLab 기본 MR 템플릿 추가 및 주요 변경/follow-up 포맷으로 개선

### Managed Resources API (#49 일부)

- managed resources 및 cluster diff API 엔드포인트 추가

---

## 요약

> [!summary] 주요 성과
> 이 프로젝트에서 가장 밀도 높은 하루. 장애 감지 시스템(sliding-window threshold + notification, #51/#52) 완성, K8s manifest 검증 체계(#71) 구축, 배포 성공 판정을 consecutive readiness 기반으로 강화. SSE 실시간 전파 개선, 설정 네임스페이스 마이그레이션까지 병행.
