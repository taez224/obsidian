---
date: 2026-04-09
tags:
  - 개발/DevLog
projects:
  - qra-backend
---

# 2026-04-09 DevLog

> [!note] 출처
> 부모 세션 유실(30일 보존 정책 초과). git 커밋(author = `Taejun Heo`) 1차 사료로 재구성.
> 내부 timestamp가 해당 날짜와 일치하는 세션·서브에이전트 트랜스크립트 0건. 작성자는 Taejun Heo 단독 범위로 한정.

## qra-backend — dead code 대청소 + v7.1.0 릴리스 준비

### #226 — 미사용 경로(dead subgraph) 제거
- **Redeploy 골격 제거 및 ArgoCD restart 우회 경로 정리** (13 files, +55 / **-584**) — 진입점에서 더 이상 호출되지 않는 하위 그래프 통째 제거
- **단건 Deployment 제거 골격 정리** (3 files, -88)
- **deprecated `UPDATE_REMOVAL` 경로 제거** (3 files, -36)
- release 브랜치를 feature/issue-226에 머지(14:02)해 정리 기준선 동기화

### application.yml 배포 설정 정비
- **미사용 SSE config 제거** + Flyway/JPA 설정 갱신 (-10)
- Flyway 정리: placeholders/default schema/schemas 제거, `baseline-on-migrate` 조정, 미사용 user/password 프로퍼티 제거

### v7.1.0 릴리스 준비
- `.gitlab-ci.yml` **`BASE_VERSION` 7.1.0 bump**
- v7.1.x 파이프라인 테스트

### 문서 / 툴링
- 배포 흐름 문서 정제 + deprecated 경로 문서 제거 (11 files)
- (툴링) `daily` 커맨드 문서(Telegram/Claude Code) 임시 추가 — qra 제품 코드 아님

---

## 요약

> [!summary] 주요 성과
> **정리-릴리스 묶음의 하루.** #226에서 더 이상 쓰이지 않는 경로(Redeploy 골격, ArgoCD restart 우회, 단건 Deployment, deprecated `UPDATE_REMOVAL`)를 dead subgraph 단위로 추적해 **순삭제 ~700줄** 제거. 이어 application.yml의 SSE/Flyway 설정을 단순화하고 **`BASE_VERSION`을 7.1.0으로 bump**해 마이너 릴리스를 준비. 배포 흐름 문서도 deprecated 경로를 걷어내며 동기화.

## 학습 & 인사이트
- **dead subgraph 추적**: 단일 메서드가 아니라 "진입점에서 도달 불가능한 호출 하위 그래프 전체"를 추적해 제거하는 게 핵심. Redeploy 골격을 지우면 그에 매달린 restart 우회·단건 Deployment 경로까지 함께 죽으므로 한 묶음으로 들어냄(-584가 한 커밋에 모인 이유).
- **정리 → 설정 단순화 → 버전 bump 순서**: 죽은 경로 제거 후 그 경로가 쓰던 설정(SSE config, Flyway placeholder)을 정리하고, 정합성이 맞은 상태에서 7.1.0으로 올림. 청소가 선행돼야 릴리스 diff가 깨끗함.
- **삭제도 산출물**: -700줄짜리 커밋들은 기능 추가가 없어도 유지보수성·릴리스 위생 측면의 명확한 성과 — DevLog에서 add뿐 아니라 delete 규모도 성과로 기록.
