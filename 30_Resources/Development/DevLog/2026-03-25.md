---
date: 2026-03-25
tags:
  - 개발/DevLog
projects:
  - qra-backend
---

# 2026-03-25 DevLog

> [!note] 출처
> 부모 세션 유실(30일 보존 정책 초과). git 커밋(author = `Taejun Heo`) 1차 사료로 재구성.
> 서브에이전트 트랜스크립트는 내부 timestamp가 03-25와 일치하는 항목이 0건이라 근거에서 제외(파일 mtime만 당일이었던 오탐). 작성자는 Taejun Heo 단독 범위로 한정.

## qra-backend

### #202 — developer(dvp) 배포 조회 API (진행 중, wip)
- 개발자(dvp) 진입점용 **읽기 전용 배포 조회 API** 골격 구현 (11 files, +568)
- `DistributionDvpSeek` 신설 — 조회 유스케이스 처리(Seek 계층)
- `DistributionDvpSeekFacade` / `DistributionDvpSeekResource` — REST 진입점 추가
- 6종 `*DvpFetch` 추가: ContainerRegistries/ContainerRegistry, Deployments/Deployment, GitOpsRepositories/GitOpsRepository — FetchRequest 기반 조회 패턴
- `ContainerRegistryRdo` / `GitOpsRepositoryRdo` 응답 모델 필드 보강
- 미완(wip) 상태로 커밋 후 `main`을 feature 브랜치로 머지해 동기화

### #200 — QraRoles 역할 체계 정비
- `QraRoles`를 **3 GroupRole + 4 Feature Role** 체계로 재정비
- `requirement-v2.md`에 역할 체계 반영(+77)
- 신규 가이드 문서 `docs/guide/vizend/prologue-role-matching.md` 작성(258줄) — Prologue 역할 매칭 규칙 정리
- `prologue-auth-headers.md` 연계 갱신
- feature/issue-200 → `main` 머지 완료

### #174 — US-9.3 정제 (ConfigMap/Secret 마운트)
- US-9.3 AC를 **ConfigMap/Secret mount 처리**가 명확해지도록 정제 (requirement-v2.md, implementation-status)
- 관련 Flyway 마이그레이션 `V7_0_25__Add_image_ref_to_deployment.sql` 한 줄 보정

### #196 — Deployment resolved imageRef 영속화 (머지)
- 전일(03-24) 구현분이 당일 새벽 `main`에 머지됨
- `Deployment` 엔티티에 resolved `imageRef`를 영속화 — `DeploymentCdo`/`DeploymentJpo`, `V7_0_25` 마이그레이션
- DeploymentFlow/Action/Task 리팩토링 동반(`RepoGitOpsQueuedEvent` 정리, Preparation/Attempt Action 간소화)과 테스트 정비(19 files, +313 / -253)

---

## 요약

> [!summary] 주요 성과
> 조회 API + 역할 체계 정비일. developer(dvp) **배포 조회 API 골격(#202)**을 Seek 계층으로 분리 착수(11 files, +568, wip)하고, **QraRoles를 3 GroupRole + 4 Feature Role 체계로 정비(#200)**하며 prologue-role-matching 가이드를 SSOT로 작성. US-9.3(ConfigMap/Secret 마운트) 요구사항을 정제(#174)하고, 전일 구현한 **Deployment imageRef 영속화(#196)**를 main에 머지.

## 학습 & 인사이트
- **DevLog mtime 오탐 검증**: `-newermt`로 잡힌 서브에이전트 15건은 내부 `"timestamp":"2026-03-25"` grep 결과 0건 → 파일 mtime은 무관한 작업(git 체크아웃 등)으로 갱신되므로 신뢰 불가. 콘텐츠 timestamp가 ground truth.
- **조회 유스케이스 분리**: #202에서 상태 변경(Flow) 대신 **Seek 계층**으로 dvp 조회 API를 분리 — 아키텍처 불변식(조회는 Seek, 변경은 Flow) 준수.
- **역할 체계 문서화 동반(#200)**: 코드(QraRoles) 변경과 동시에 prologue-role-matching 가이드를 함께 작성해 역할 매칭 규칙을 SSOT화.
