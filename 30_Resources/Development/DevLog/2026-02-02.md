---
date: 2026-02-02
tags:
  - 개발/DevLog
projects:
  - vizend-qra-poc
  - vizend-qra-backend
---

# 2026-02-02 DevLog

## vizend-qra-poc

### GitOps 로컬 개발 가이드 문서 작업

- `gitops-local-dev-guide.md` 질문 대응 내용 정리
- Docker Compose에서 MinIO/Redis 제거 (불필요한 의존성 정리)
- `restart: unless-stopped` 옵션 학습
- ArgoCD Web UI 방식 가이드 추가 (터미널 커맨드 외 대안)

### 통합 테스트 구현

- Testcontainers 설정 섹션 실제 구현 코드에 맞게 업데이트
  - PostgreSQL → H2 인메모리 (PostgreSQL 호환 모드)
  - Redis → NoOp Mock
  - `AbstractE2ETestBase` 상속 패턴 반영
- E2E 테스트 구조 설계 및 구현 계획 수립
  - Kafka TestContainers + H2 + JGit 조합
  - Subscription/Update/Unsubscribe 플로우 테스트

### Gallery Migration 구현

- `qra-gallery-migration-plan.md` 바탕으로 실제 구현 진행 (Sisyphus 모드)
- Subscription 엔티티 관련 설계 수정
  - Subscription/SubscriptionState/DeploymentState 엔티티 삭제 (Gallery 담당)
  - OperationHistory만 유지

### 기타

- Chrome vs Safari 브라우저 ArgoCD 접속 차이 트러블슈팅
- ArgoCD Pod Terminating 상태 확인
- Windows 환경 가이드 주석 추가

---

## vizend-qra-backend

### 문서 구조 정비

- `docs/domain-model/qra-domain.md` 템플릿 구조 적용
- `docs/architecture/` 문서들 구조 통일
  - "문서 정보" 섹션 제거
  - "변경 이력" 상단 이동
  - 변경 이력: 버전 제거, 날짜/내용/담당자(git username) 형식

### ADR 검증 및 보완

- ADR-003 (K8S Observation Strategy) 검증
  - Fabric8 6.13.5 버전 선정 근거 추가 (Spring Boot 3.2/Jackson 2.15.x 호환)
  - Fabric8 6.14.0+ 은 Jackson 2.19.0+ 필요 (비호환)
- Jackson 관련 ADR 내용 추가

### AGENTS.md 체계 구축

- `docs/architecture/AGENTS.md` 생성 (아키텍처 문서 가이드라인)
- `docs/guide/AGENTS.md` 생성 (가이드 문서 가이드라인)
- `docs/adr/AGENTS.md` 생성 (ADR 구조 가이드라인)
- `docs/domain-model/AGENTS.md` 생성 (도메인 모델 문서 가이드라인)
- `docs/AGENTS.md` 생성 (공통 변경 이력 규칙)
- 모든 CLAUDE.md → AGENTS.md 변경 + 심볼릭 링크 생성

### 커스텀 명령어

- `/sync-domain-docs` 명령어 생성 (도메인 모델 코드-문서 동기화)
  - qra-domain 코드 분석 → qra-domain-models.md 자동 업데이트
  - 최종 동기화 날짜 관리

### 도메인 모델 문서화

- `docs/domain-model/qra-domain-models.md` 작성
  - Entity/VO/Enum 정의
  - Mermaid ER 다이어그램
  - 내부 VO/Enum 앵커 링크 연결
  - 용어 사전 포함

### PRD 작업

- Prometheus 플래닝으로 PRD 사전 작업
  - Technical PRD, Business PRD, Technical Spec 3개 문서 계획
  - EPIC 1-9 범위 정의 (EPIC-9: CI/CD Autodeploy)
  - Gallery autodeploy 기능 QRA 이관 계획

### 병렬 검증 (Ultrawork)

- `docs/` 회의/분석 자료와 `qra-domain/` 도메인 모델링 병렬 검증

### 기타

- `.gitignore` 정리 (.settings 등 IDE 파일 추가)
- PlantUML vs Mermaid 비교 검토
- Codex/Cursor 커스텀 커맨드 호환성 조사
- GitLab PlantUML 지원 확인

---

## 학습 & 인사이트

- **Fabric8 버전 호환성**: Spring Boot 3.2는 Jackson 2.15.x 사용, Fabric8 6.13.5까지 호환
- **AGENTS.md 패턴**: Codex/Cursor 등 다른 AI 도구와 호환을 위해 AGENTS.md + CLAUDE.md 심볼릭 링크 패턴 적용
- **문서 유형 분리**: Guide(어떻게) vs Architecture(왜) vs ADR(단일 결정) 명확한 책임 분리
- **KRaft 모드**: Kafka 3.x에서 ZooKeeper 불필요, 단일 프로세스로 운영 복잡도 감소