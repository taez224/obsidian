---
date: 2026-03-17
tags:
  - 개발/DevLog
projects:
  - vizend-qra-backend
---

# 2026-03-17 DevLog

## vizend-qra-backend

### GitOps 큐 중복 실행 방지 (#171)

- Batch Dispatch Lease + Update Reconcile 통합으로 GitOps 큐 중복 실행 방지
- `DeployRequest` 6-param 생성자 제거 — `requestId` 유실 방지 버그 수정
- GitOps dispatch lease 테스트 추가

### Kollex 배포 제외(EXCLUDED) 상태 도입 (#168)

- 배포 제외 상태(`EXCLUDED`) 신규 도입
- 전 경로(배포 요청 ~ 결과 처리)에 걸쳐 EXCLUDED 상태 적용

### K8s Pod 조회 namespace batch 최적화 (#167)

- `K8sPodStatusProxy`에 namespace 단위 batch Pod 조회 메서드 추가
- `RuntimeMonitoringSeek`에서 namespace별 batch 조회 결과를 method-scope 캐시로 재사용
- 동일 namespace 복수 deployment에 대해 K8s API 호출 1회로 최적화

### 인프라 튜닝

- async executor 및 DB connection pool 스레드 풀 사이즈 조정
- executor 설정값 최적화

---

## 요약

> [!summary] 주요 성과
> GitOps 큐 안정성(lease 기반 중복 방지), K8s 조회 성능 최적화(namespace batch), Kollex 배포 제외 상태 도입. 인프라 레벨 스레드 풀 튜닝까지 진행한 하루.
