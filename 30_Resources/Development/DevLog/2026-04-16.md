---
date: 2026-04-16
tags:
  - 개발/DevLog
projects:
  - qra-backend
---

# 2026-04-16 DevLog

> [!note] 출처
> 부모 세션 유실(30일 보존 정책 초과). git 커밋(author = `Taejun Heo`) 1차 사료로 재구성.
> 내부 timestamp가 해당 날짜와 일치하는 세션·서브에이전트 트랜스크립트 0건. 작성자는 Taejun Heo 단독 범위로 한정.

## qra-backend — GitOps 토큰 보존 버그픽스

### #229 — GitOps Repository modify 시 gitOpsServer 토큰 보존
- GitOps Repository **수정 시 `gitOpsServer` 토큰이 유실되던 버그 수정** (2 files, +146 / -78)
- 부분 수정 요청에 토큰이 빠져 있으면 **기존 값을 보존**하도록 보강 — modify가 토큰을 빈 값으로 덮어쓰지 않게

---

## 툴링 / 메타 (qra 제품 코드 아님)

### AI 하네스 정비
- `.claude/skills`·`commands`·`WORKFLOW.md`·`settings.json`·`AGENTS.md` 등 **Claude Code 하네스 대규모 리팩토링** (42 files, +2384 / -1517)
- review-implement·telegram-channels·implement-task·create-issue 등 스킬/커맨드 정비

---

## 요약

> [!summary] 주요 성과
> GitOps Repository **수정 시 인증 토큰이 사라지던 버그(#229)를 수정**해, 부분 수정에서 누락된 token을 기존 값으로 보존하도록 보강. 더불어 AI 개발 하네스(.claude 스킬/커맨드/워크플로)를 대규모 정비(+2384/-1517).

## 학습 & 인사이트
- **부분 수정의 민감 필드 보존**: modify 요청에 토큰처럼 쓰기 전용/민감 필드가 비어 오면, 빈 값으로 덮어쓰지 말고 기존 영속 값을 보존해야 함 — "PATCH 시맨틱"의 전형적 함정. token은 응답에 노출도 안 되니 비어 오는 게 정상 케이스.
- **제품 코드와 하네스 작업의 분리 기록**: 같은 날 42-file 변경은 `.claude` 툴링이라 제품 성과와 섞으면 안 됨 — DevLog에서 메타 작업은 별도 섹션으로 구분(이력서 귀속 기준에서도 구분 필요).
